apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miso-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "miso-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miso-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
